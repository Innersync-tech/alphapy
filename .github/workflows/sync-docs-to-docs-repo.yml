name: Sync Alphapy docs to docs repo (DISABLED — Starlight archived)

# Sprint 3: Mintlify is docs SoT. Use sync-docs-to-mintlify.yml instead.
# Kept disabled so accidental pushes do not revive Starlight content.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  sync-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "Starlight docs sync is disabled. Canonical target: Innersync-tech/mintlify-docs"
          echo "See .github/workflows/sync-docs-to-mintlify.yml"
          exit 0
